import React from "react";
import { StyleSheet, View, Text } from "react-native";

const ProfileProjects = ({}) => {
    return (
        <View>
            <Text>Profile Projects</Text>
        </View>
    );
}

export default ProfileProjects