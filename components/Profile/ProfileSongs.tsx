import React from "react";
import { StyleSheet, View, Text } from "react-native";

const ProfileSongs = ({}) => {
    return (
        <View>
            <Text>Profile Songs</Text>
        </View>
    );
}

export default ProfileSongs