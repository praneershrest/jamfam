import React from "react";
import { StyleSheet, View, Text } from "react-native";

const ProfileSnippets = ({}) => {
    return (
        <View>
            <Text>Profile Snippets</Text>
        </View>
    );
}

export default ProfileSnippets